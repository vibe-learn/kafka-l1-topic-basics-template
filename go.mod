module github.com/vibe-learn/kafka-l1-topic-basics

go 1.22

// Student adds production Kafka client when ready, e.g.:
//   require github.com/segmentio/kafka-go v0.4.47
//
// Author tests in producer_test.go use only stdlib + MessageWriter interface,
// so no external deps are required for `go test ./...` to compile.
